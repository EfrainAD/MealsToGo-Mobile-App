import React from 'react'
import { Text, StyleSheet } from 'react-native'

export const RestaurantInfo = () => (
   <Text style={styles.listItem}>Restaurant Info</Text>
)

const styles = StyleSheet.create({
   listItem: { color: 'white' },
})
